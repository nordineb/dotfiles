# dotfiles

## Brewfile

Dump all currently installed software into a new Brewfile file
```sh
brew bundle dump
```

Install
```sh
brew bundle --file ~/my-folder/Brewfile
```

## asdf

```sh
cd ~
asdf install
````
